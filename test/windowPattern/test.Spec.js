


parse = require('chai');
var assert = chai.assert;
describe("windowPattern", function() {
  
    it("for negative n the result is NaN", function() {
      assert.isNaN(2);
    });
  
    it("for non-integer n the result is NaN", function() {
      assert.isNaN(10);
    });
  
  });
  

